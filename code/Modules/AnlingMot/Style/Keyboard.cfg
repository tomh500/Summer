bind +ForwardEvent +forward
bind -ForwardEvent -forward

bind +BackEvent +back
bind -BackEvent -back

bind +RightEvent +right
bind -RightEvent -right

bind +LeftEvent +left
bind -LeftEvent -left